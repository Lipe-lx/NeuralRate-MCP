{
  "name": "neuralrate-mcp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "contracts",
    "packages/*"
  ],
  "scripts": {
    "install:all": "npm install",
    "sync:deployments": "node scripts/sync-deployments-to-env.mjs"
  }
}
